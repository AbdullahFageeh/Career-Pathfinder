# Optional OpenAI-compatible letter refinement.
# Keep the key empty unless you explicitly want network-backed rewriting.
LLM_API_KEY=
LLM_API_BASE=https://api.openai.com/v1

# Optional supervised Greenhouse Job Board API submission.
# The default configuration remains observe-only without this value.
GREENHOUSE_JOB_BOARD_API_KEY=
