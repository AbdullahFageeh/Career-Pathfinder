{
  "name": "career-pathfinder",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Saudi-first, evidence-bound career discovery and application workflow.",
  "engines": {
    "node": ">=22 <25"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/AbdullahFageeh/Career-Pathfinder.git"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "npm run typecheck && npm run privacy:scan && npm test",
    "clean": "node -e \"require('node:fs').rmSync('dist', { recursive: true, force: true })\"",
    "privacy:scan": "node scripts/privacy/check-fixtures.mjs",
    "test": "npm run build && node --test dist/**/*.test.js",
    "typecheck": "tsc --noEmit",
    "start": "node dist/server.js",
    "start:cli": "node dist/index.js",
    "greenhouse:hosted:prefill": "npm run build && npm run start:cli -- greenhouse:hosted:prefill",
    "discover:greenhouse": "npm run build && npm run start:cli -- discover:greenhouse",
    "automation:run": "npm run build && npm run start:cli -- automation:run",
    "shortlist": "npm run build && npm run start:cli -- shortlist",
    "cv": "npm run build && npm run start:cli -- cv",
    "review:packets": "npm run build && npm run start:cli -- review:packets",
    "letter": "npm run build && npm run start:cli -- letter",
    "followups": "npm run build && npm run start:cli -- followups",
    "report": "npm run build && npm run start:cli -- report",
    "pipeline:single": "npm run build && npm run start:cli -- pipeline:single",
    "queue:single": "npm run build && npm run start:cli -- queue:single",
    "worker:once": "npm run build && npm run start:cli -- worker:once"
  },
  "devDependencies": {
    "@types/node": "^22.15.30",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "playwright-core": "^1.60.0"
  }
}
